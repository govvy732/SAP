{
  "$schema": "sap://tokens.schema.json",
  "_comment": "Bundled known token list for SAP. If pharos-skill-engine is installed at the framework's skills root, its tokens.json takes precedence. Add token entries below as `{address, symbol, decimals, name}` (all strings for symbol/name, integer for decimals). The address is the lowercase ERC-20 contract address on Pharos mainnet.",
  "tokens": []
}
